def calculate_motorcycle_speeds(average_speed, distance, time_A_to_B):
  """
  Вычисляет среднюю скорость мотоциклиста из A в B (V1) и из B в A (V2).

  Args:
    average_speed: Средняя скорость движения на всем пути (А -> B -> A) в км/ч.
    distance: Расстояние между пунктами А и B в км.
    time_A_to_B: Время движения из пункта А в пункт B в часах.

  Returns:
    Кортеж (V1, V2), где:
      V1: Средняя скорость из пункта А в пункт B в км/ч.
      V2: Средняя скорость из пункта B в пункт А в км/ч.
      None, None: Если невозможно вычислить V2 (например, при делении на ноль).
  """

  V1 = distance / time_A_to_B  # Скорость из A в B

  # Проверяем, чтобы average_speed не был равен 0 (чтобы избежать деления на 0)
  if average_speed == 0:
    return None, None

  time_total = (2 * distance) / average_speed  # Общее время в пути (А -> B -> A)
  time_B_to_A = time_total - time_A_to_B  # Время движения из B в A

  # Проверяем, чтобы время из B в A было положительным числом. Иначе V2 не имеет смысла.
  if time_B_to_A <= 0:
    return None, None

  V2 = distance / time_B_to_A  # Скорость из B в A

  return V1, V2


# Пример использования:
average_speed = 60  # км/ч
distance = 120  # км
time_A_to_B = 2  # часа

V1, V2 = calculate_motorcycle_speeds(average_speed, distance, time_A_to_B)

if V1 is not None and V2 is not None:
  print(f"Средняя скорость из A в B (V1): {V1:.2f} км/ч")
  print(f"Средняя скорость из B в A (V2): {V2:.2f} км/ч")
else:
  print("Невозможно вычислить скорости. Проверьте входные данные.")
